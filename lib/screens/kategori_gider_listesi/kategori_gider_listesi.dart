import 'package:flutter/material.dart';

class KategoriGiderListesi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Kategori Gider Listesi"),
    );
  }
}

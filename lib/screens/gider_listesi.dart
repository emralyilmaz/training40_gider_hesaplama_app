import 'package:flutter/material.dart';

class GiderListesi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Gider Listesi"),
    );
  }
}
